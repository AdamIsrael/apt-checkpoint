* Implement --clean
* Implement --version
* Allow rollback for any previous checkpoint, not just the last one
* Look into archiving of conf files
* Implement --debug and verbose output
* Handle packages in alternative, non-official repositories
* Internationalization
* Test on multiple platforms (should work fine, but check packaging)
* Check permissions on create/rollback; requires root privs
